pluginManagement {
    plugins {
        id "idea"
        id 'org.asciidoctor.jvm.convert' version '3.3.2'
    }
    repositories {
        mavenCentral()
        maven { url = 'https://plugins.gradle.org/m2/' }
    }
}
rootProject.name = 'nf-plugin-generator-cli'